<UserControl
    x:Class="Mvvm.Views.ParameterWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:Mvvm.Converters"
    xmlns:local="clr-namespace:Mvvm.Views"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:prism="http://prismlibrary.com/">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Dictionaries/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <converters:StringToIntConverter x:Key="StringToIntConverter" />
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="100" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  설정 영역  -->
        <StackPanel
            Grid.Row="0"
            Margin="10"
            Orientation="Horizontal">
            <Label VerticalAlignment="Center" Content="Count" />
            <TextBox
                x:Name="Parametercount"
                Width="100"
                Text="{Binding ParameterCount}" />
            <Label VerticalAlignment="Center" Content="Start" />
            <TextBox
                x:Name="StartAddressTextBox"
                Width="100"
                Text="{Binding StartAddress, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource StringToIntConverter}}" />
            <Label VerticalAlignment="Center" Content="End" />
            <TextBox
                x:Name="EndAddressTextBox"
                Width="100"
                Text="{Binding EndAddress, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource StringToIntConverter}}" />
            <Button
                Width="80"
                Margin="10,0,0,0"
                Command="{Binding ApplyCommand}"
                Content="Apply" />
            <!--  카드 형식 체크박스  -->
            <CheckBox
                x:Name="isCardView"
                Margin="10,0,0,0"
                Content="형식"
                IsChecked="{Binding IsCardView, Mode=TwoWay}"
                Style="{StaticResource MaterialDesignCheckBox}" />
            <!--  템플릿 갱신 버튼  -->

        </StackPanel>

        <!--  리스트 or 카드 형식 선택  -->
        <ScrollViewer
            Grid.Row="1"
            Grid.ColumnSpan="2"
            Margin="10"
            HorizontalScrollBarVisibility="Auto"
            VerticalScrollBarVisibility="Visible">
            <ItemsControl x:Name="ParameterItemsControl" ItemsSource="{Binding Parameters}">
                <ItemsControl.ItemTemplateSelector>
                    <local:ParameterTemplateSelector CardTemplate="{StaticResource CardTemplate}" ListTemplate="{StaticResource ListTemplate}" />
                </ItemsControl.ItemTemplateSelector>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="{Binding Columns}" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
